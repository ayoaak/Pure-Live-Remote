# Pure Live Remote

Server-side companion project for Pure Live.

## Branch policy

- `main`: only changes that have passed validation and user acceptance.
- `Test`: all new, experimental, or not-yet-validated changes.

Current development starts on `Test`.
